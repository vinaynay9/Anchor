// DailyAggregate is defined in AppGroupStorage.swift so it compiles as part
// of the Shared target alongside the storage methods that use it.
// This file is intentionally empty to avoid duplicate definition errors
// if it gets added to the Xcode project.
