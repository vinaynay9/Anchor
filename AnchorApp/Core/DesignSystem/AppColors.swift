import SwiftUI

struct AppColors {
    // Primary colors
    static let primary = Color(red: 0.2, green: 0.4, blue: 0.8)
    static let primaryDark = Color(red: 0.1, green: 0.3, blue: 0.7)
    
    // Accent colors
    static let accent = Color(red: 0.9, green: 0.3, blue: 0.2)
    static let accentLight = Color(red: 0.95, green: 0.5, blue: 0.4)
    
    // Background colors
    static let background = Color(.systemBackground)
    static let secondaryBackground = Color(.secondarySystemBackground)
    
    // Text colors
    static let textPrimary = Color(.label)
    static let textSecondary = Color(.secondaryLabel)
    
    // Status colors
    static let success = Color.green
    static let warning = Color.orange
    static let error = Color.red
    
    // Shield screen colors
    static let shieldBackground = Color(red: 0.1, green: 0.1, blue: 0.15)
    static let shieldText = Color.white
}

