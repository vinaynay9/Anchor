import Foundation
import Shared

// QuorumState is defined in the Shared framework.
// Re-export it here as a type alias so AnchorApp code can use it without the module qualifier.
typealias QuorumState = Shared.QuorumState
